% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tensorlake_to_markdown.R
\name{tensorlake_to_markdown}
\alias{tensorlake_to_markdown}
\title{Convert Tensorlake Result to Markdown}
\usage{
tensorlake_to_markdown(
  result,
  pages = NULL,
  include_page_breaks = TRUE,
  include_headers = FALSE,
  include_footers = FALSE
)
}
\arguments{
\item{result}{List. The result object from tensorlake_ocr() or tensorlake_get_parse_result().}

\item{pages}{Integer vector. Optional page numbers to convert. Default is NULL (all pages).}

\item{include_page_breaks}{Logical. Include page break markers in output. Default is TRUE.}

\item{include_headers}{Logical. Include page headers in output. Default is FALSE.}

\item{include_footers}{Logical. Include page footers/numbers in output. Default is FALSE.}
}
\value{
Character string containing the Markdown-formatted document.
}
\description{
Convert a Tensorlake parsing result to well-formatted Markdown, preserving
document structure including headers, text, tables, and figures.
}
\examples{
\dontrun{
result <- tensorlake_ocr("document.pdf")

# Convert to markdown
markdown <- tensorlake_to_markdown(result)
cat(markdown)

# Save to file
writeLines(markdown, "document.md")

# Convert only first 5 pages without page breaks
markdown <- tensorlake_to_markdown(result, pages = 1:5, include_page_breaks = FALSE)
}

}
